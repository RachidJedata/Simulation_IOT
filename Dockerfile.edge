FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY edge/ ./edge/
COPY config/ ./config/
CMD ["python3", "edge/simulator.py", "--config", "config/simulation.json"]
